<UserControl x:Class="Battleships.MVVM.View.CustomControls.InformationPopUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battleships.MVVM.View.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <ToggleButton Width="30" Height="30"
                      x:Name="infotogglebutton"
                      Background="Transparent">
            <ToggleButton.Template>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource MainText}"
                            BorderThickness="2"
                            CornerRadius="15">
                        <TextBlock Text="?"
                                   FontFamily="Stencil"
                                   FontWeight="Normal"
                                   FontSize="20"
                                   Foreground="{DynamicResource MainText}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="{DynamicResource StandardButtonBackground}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="{DynamicResource IsHighlightedBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
        <Popup x:Name="infopopup"
               IsOpen="{Binding IsChecked, ElementName=infotogglebutton}"
               StaysOpen="False">
            <Viewbox Stretch="Uniform">
                <Grid Height="450"
                      Width="800"
                      Background="{DynamicResource MainBackground}">
                    <Image Source="{Binding Image, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue={x:Null}}"
                           Stretch="Uniform"/>
                </Grid>
            </Viewbox>
        </Popup>
    </StackPanel>
</UserControl>
