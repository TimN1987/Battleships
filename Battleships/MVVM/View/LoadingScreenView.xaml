<UserControl x:Class="Battleships.MVVM.View.LoadingScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battleships.MVVM.View"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" MinHeight="600" MinWidth="800">
        <Grid.Background>
            <ImageBrush ImageSource="Resources/backgroundWaves.jpg"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="1" Grid.Column="1" CornerRadius="80, 80, 80, 80" BorderBrush="Navy" BorderThickness="1">
            <Border.Background>
                <VisualBrush>
                    <VisualBrush.Visual>
                        <Image gif:ImageBehavior.AnimatedSource="Resources/loadingGIF.gif"
                               gif:ImageBehavior.AutoStart="True"
                               gif:ImageBehavior.RepeatBehavior="3x"
                               gif:ImageBehavior.AnimationSpeedRatio="0.5"/>
                    </VisualBrush.Visual>
                </VisualBrush>
            </Border.Background>
        </Border>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="Battleships" 
                       Foreground="AntiqueWhite" FontSize="80" FontWeight="Bold" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="2" Grid.ColumnSpan="3" Text="Command the fleet. Rule the seas."
                       Foreground="AntiqueWhite" FontSize="30" FontStyle="Italic"
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Border Grid.Row="3" Grid.Column="1" MaxHeight="25" MaxWidth="400"
                    BorderBrush="Navy" BorderThickness="1" Background="PowderBlue">
            <Rectangle Name="loadingRectangle" VerticalAlignment="Center" HorizontalAlignment="Left"
                       MinHeight="25" Width="5" Fill="Navy">
                <Rectangle.Triggers>
                    <EventTrigger RoutedEvent="Rectangle.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="loadingRectangle" Storyboard.TargetProperty="Width"
                                                 From="5" To="400" Duration="0:0:5">
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Rectangle.Triggers>
            </Rectangle>
        </Border>
        <TextBlock Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                   Text="Loading..." Foreground="GhostWhite" FontSize="15" FontWeight="Thin"/>
    </Grid>
</UserControl>
