<Window x:Class="Battleships.MVVM.View.LoadingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Battleships.MVVM.View"
        xmlns:viewmodel="clr-namespace:Battleships.MVVM.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:LoadingViewModel}"
        mc:Ignorable="d"
        ResizeMode="NoResize" Height="450" Width="800">
    <Grid>
        <Viewbox>
            <TextBlock Text="{Binding Progress}"/>
        </Viewbox>
        <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}" />
    </Grid>
</Window>
