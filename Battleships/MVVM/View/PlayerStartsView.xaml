<UserControl x:Class="Battleships.MVVM.View.PlayerStartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battleships.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Viewbox>
            <Grid Width="800" Height="450">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="75"/>
                    <RowDefinition Height="75"/>
                    <RowDefinition Height="75"/>
                    <RowDefinition Height="75"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="1"
                           Text="{Binding PlayerStartsViewTitle}"
                           Style="{DynamicResource TitleText}"/>
                <Button Grid.Row="3"
                        Content="{Binding PlayerStartsButtonText}"
                        Style="{DynamicResource StandardButtonStyle}"
                        Command="{Binding PlayerStartsCommand}"
                        Width="250"
                        Margin="0,10"/>
                <Button Grid.Row="4"
                        Content="{Binding ComputerStartsButtonText}"
                        Style="{DynamicResource StandardButtonStyle}"
                        Command="{Binding ComputerStartsCommand}"
                        Width="250"
                        Margin="0,10"/>
                <Button Grid.Row="5"
                        Content="{Binding RandomStartsButtonText}"
                        Style="{DynamicResource StandardButtonStyle}"
                        Command="{Binding RandomStartsCommand}"
                        Width="250"
                        Margin="0,10"/>
            </Grid>
        </Viewbox>   
    </Grid>
</UserControl>
